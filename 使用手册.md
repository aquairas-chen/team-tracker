# 📘 团队进度看板 · 使用手册

> 适用于：全体成员（含管理员 Jiaxin、管理员 Kimi 助理）
> 版本：v5（含 @提醒 / 全员动态 / 评论附件 / 预警闭环）
> 访问地址：GitHub Pages 团队追踪链接（通过邀请卡二维码进入）

---

## 一、快速开始

1. **打开看板**：微信内扫邀请卡二维码，或点击管理员分享的链接。
2. **输入访问密码**：首次进入需输入团队访问密码（找管理员获取）。
3. **选择身份**：首次进入会让你选择"我是谁"（Shimiao / Jiaxin / Hanrui）。选错可随时在顶部成员标签右侧点身份重置。
4. **建议**：微信里打开后点右上角「···」→「浮窗」或「收藏」，以后随时回到看板。

> 手机端、iPad、电脑均可访问，界面会自动适配屏幕。

---

## 二、页面导览（8 个 Tab）

| Tab | 作用 |
|---|---|
| **甘特** | 项目时间线图。按项目分组、按类型配色；虚线=今天；可拖拽条形直接改日期；左下角"导出PNG"出图存档 |
| **依赖** | 任务依赖关系图，箭头=前置关系，红点=已延期阻塞，拖节点可布局（重置按钮恢复） |
| **任务** | 全部任务卡片列表，点任意卡片打开详情抽屉 |
| **里程碑** | 关键节点（SOP / BOM / 官网 / 发布会等），点击看关联任务 |
| **我的** | 只看"我负责的"和"我处理过的"任务；底部是我的预警处置通道 |
| **思考** | Hanrui 专属备忘录（仅本人可见），支持新增/删除 |
| **动态** | 全员动作流水：谁启动/完成/预警/评论/改期了什么，按今天/昨天/日期分组 |
| **提醒** | 顶部"📣@我的"定向提醒 + 未启动/临期/延期/停滞四类风险任务 + 未处置预警 |

> 🔴 红色角标：Tab 栏「提醒」上的红点数字 = 风险任务数 + 未处置预警数 + 未读@数。开始任务进度会自动消除对应计数。

---

## 三、成员日常操作（所有人都会用的）

### 3.1 点任务卡片 → 详情抽屉里能做什么

- **▶️ 开始**：任务从"未开始"变为"进行中"（进度自动设为 5%），同时消除提醒红点。
- **✔ 完成**：提交完成记录，进度到 100%，全员动态里会留痕。
- **⚠️ 预警**：遇到问题点它，填写原因后提交 —— 会实时推送微信给管理员，卡片变为红色"⚠ 预警中"。
- **💬 评论**：可写文字、**@成员**（点输入框下方的 @快捷按钮或直接打 `@名字`）、**添加附件**（图片/文件）。评论会永久留痕。
- **进度条**：管理员可直接拖动；普通成员的进度由"开始/完成"动作驱动。

### 3.2 被别人 @ 了会怎样

打开看板 → 「提醒」Tab 顶部出现蓝色卡片 **"📣 @我的"**，显示谁在哪个任务里@了你、说了什么；看完点 **「知道了 ✓」** 消除。未读的@会计入提醒红点。

### 3.3 查看其他人做了什么

「动态」Tab 按时间倒序列出全员动作：开始 ✅、完成 🎉、预警 ⚠️、评论 💬、管理员改期 ✏️、备忘 📝 等，点任务名可直接跳到该任务。

---

## 四、管理员操作指南（Jiaxin & 管理员）

### 4.1 编辑任务（改时间/加任务/加阶段）

- 任务卡片右上角 **✎** 按钮 → 弹窗修改：任务名、日期、天数、详情、优先级、预估工时、依赖、负责人。
- **新增任务**：任务 Tab 右上角 **＋**；**新增阶段**：阶段标题右侧 **＋**。
- **拖拽改期**（更快）：甘特图里直接拖动条形，松手自动保存并同步全员。

### 4.2 发布 / 同步机制

- 任何修改（编辑/拖拽/发布）都会**即时写入云端**，其他成员**刷新页面即看到最新版**（成员端每 30 秒也会自动拉取一次）。
- 非管理员成员看到的编辑按钮是禁用的，只有管理员身份（Jiaxin / 管理员设备）可改。

### 4.3 预警处置（管理员专属）

- 收到微信推送或看到红色"⚠ 预警中"卡片 → 「我的」Tab 底部或任务 Tab 进入 → 填写处置意见 → 任务恢复绿色进行中。
- 全部处置历史在控制档案中留痕。

### 4.4 基线 vs 实际

- 甘特图 **"📌设基线"** 按钮：把当前计划定格为基准线。
- 之后打开 **"基线"开关**：每条任务下出现灰色基准条，实际条相对它前后偏移 = 计划 vs 现实差距，一目了然。

### 4.5 快照与回滚

- 每次发布会自动保存历史版本。**⚙ 设置 → 历史快照** 可回滚到任意时间点（回滚本身也会生成快照，可反悔）。

### 4.6 成员与推送管理

- **⚙ 设置 → 成员管理**：为每位成员绑定微信推送 Key（Server酱 SendKey），该成员的预警/定向提醒会推到他/她自己的微信。
- **⚙ 设置 → 修改访问密码**：改密码后全员下次进入需用新密码。

---

## 五、每日微信自动提醒

- 每天晚上（北京时间 21:17）系统自动运行：扫描全部任务，把 **未启动清单（已过了开始日期但进度仍为 0）** 以微信消息推送给管理员。
- 也可以随时由管理员手动触发一次推送。
- 成员个人若绑定了 SendKey，预警和@提醒也会定向推给本人。

---

## 六、常见问题

**Q：我改了东西别人多久能看到？**
A：即刻写入云端；其他人的页面 30 秒内自动刷新，手动下拉/重开更快。

**Q：忘记选身份或选错了？**
A：顶部成员标签最右侧点身份重置，重新选择即可。身份只存在你自己的设备上。

**Q：能看到谁"已读"了吗？**
A：动态 Tab 记录的是动作留痕；"@我的"的"知道了"点击也会留痕，管理员可据此确认对方已读。

**Q：甘特图想存档汇报？**
A：甘特 Tab 左下「导出PNG」，导出图右下角自动带当天日期水印。

**Q：数据存在哪里，安全吗？**
A：计划数据以加密方式存放（AES-256），页面本身有访问密码；仓库中不含任何明文敏感信息。

---

*本手册由 Kimi 助理维护，随功能迭代更新。*
